## 🚀 Deploying ControlCenter Service

### Deployment Steps:
| Step | Command |
|-------|---------|
| Place `deploy.sh` in repo root or `/scripts/` | ✅ |
| `chmod +x deploy.sh` before commit | ✅ |
| `git add deploy.sh && git commit` | ✅ |
| On any server: `chmod +x deploy.sh && ./deploy.sh` | ✅ |

### What `deploy.sh` does:
- Publishes the .NET project to `/publish`
- Copies `controlcenter.service` to `/etc/systemd/system/`
- Reloads systemd
- Enables the service (auto-start on boot)
- Restarts the service
- Shows current service status
- Optionally tails live logs

### To deploy:
```bash
chmod +x scripts/deploy.sh
./scripts/deploy.sh
